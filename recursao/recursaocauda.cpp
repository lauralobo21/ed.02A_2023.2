#include <iostream>
#include <vector>

using namespace std;
    //recursão de cauda (vai até o final e volta somando)
    int vet_sum(int vet[], int size){
        if(size == 0)
            return 0;
        return vet[0] +
    }

    int main(){
        int vet[] = {3, 2, 1, 5, 6, 7, 2, 9};
    }